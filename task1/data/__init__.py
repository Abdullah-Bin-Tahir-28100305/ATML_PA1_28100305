# This empty file marks the `data/` directory as a Python PACKAGE.
# ML/software concept: without __init__.py, `from data.transforms import ...`
# would fail because Python would not treat the folder as an importable module.
# It is intentionally empty; its mere presence is what matters.
