# Marks task4/scores as a Python package so `from scores import msp, mls, ...`
# works. The four score modules each expose a score(logits, features=None) that
# returns an unknownness score (larger = more novel). Intentionally minimal.
